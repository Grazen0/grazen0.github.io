---
import { type HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'h1'>;

const { class: className, ...props } = Astro.props;
---

<h1
  class:list={[
    'my-10 text-center text-3xl leading-normal font-bold',
    className,
  ]}
  {...props}
>
  <slot />
</h1>
